<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        
        <!-- ========================================== -->
        <!-- CATEGORÍA DE MÓDULO -->
        <!-- ========================================== -->
        
        <record id="module_category_beauty_survey" model="ir.module.category">
            <field name="name">Beauty Salon Survey</field>
            <field name="description">Sistema de Encuestas y Garantías para Pop Studio</field>
            <field name="sequence">21</field>
        </record>
        
        <!-- ========================================== -->
        <!-- GRUPOS DE SEGURIDAD -->
        <!-- ========================================== -->
        
        <!-- Grupo: Usuario (puede ver sus propias encuestas) -->
        <record id="group_beauty_survey_user" model="res.groups">
            <field name="name">Survey User</field>
            <field name="category_id" ref="module_category_beauty_survey"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
            <field name="comment">Puede ver encuestas de sus propias citas</field>
        </record>
        
        <!-- Grupo: Manager (acceso completo) -->
        <record id="group_beauty_survey_manager" model="res.groups">
            <field name="name">Survey Manager</field>
            <field name="category_id" ref="module_category_beauty_survey"/>
            <field name="implied_ids" eval="[(4, ref('group_beauty_survey_user'))]"/>
            <field name="comment">Acceso completo: aprobar garantías, ver todas las encuestas, ajustar comisiones</field>
        </record>
        
        <!-- ========================================== -->
        <!-- REGLAS DE REGISTRO (Record Rules) -->
        <!-- ========================================== -->
        
        <!-- Usuarios normales solo ven citas donde son el empleado real -->
        <record id="calendar_event_survey_user_rule" model="ir.rule">
            <field name="name">Calendar Event: Survey Users - Own Appointments</field>
            <field name="model_id" ref="calendar.model_calendar_event"/>
            <field name="domain_force">[('real_employee_id.user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('group_beauty_survey_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>
        
        <!-- Managers ven todo -->
        <record id="calendar_event_survey_manager_rule" model="ir.rule">
            <field name="name">Calendar Event: Survey Managers - All</field>
            <field name="model_id" ref="calendar.model_calendar_event"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_beauty_survey_manager'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
        
    </data>
</odoo>